{
  "name": "clinical-registry-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/frontend",
    "packages/backend"
  ],
  "scripts": {
    "frontend": "npm run dev --workspace=packages/frontend",
    "backend": "npm run build --workspace=packages/backend",
    "test": "npm run test --workspaces",
    "deploy": "npm run deploy --workspace=packages/backend"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
